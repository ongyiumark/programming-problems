#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

vector<int> T, S;
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t, s; cin >> t >> s;
    T.resize(t); S.resize(s);
    

    return 0;
}